interface ShouldNotAppear {
	shouldNotAppear: string
}

describe('test', () => {
	it('test', () => {
		console.warn("SWCRC Setting", process.env.SWCRC)
		console.warn("TESTFLAG Setting", process.env.TESTFLAG)
	})
})
